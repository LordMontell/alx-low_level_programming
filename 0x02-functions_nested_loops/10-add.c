#include "main.h"
/**
  * add - add two integers together
  * @f: first integer argument
  * @s: second integer argument
  * Return: total of two integers
  */

int add(int f, int s)
{
	int total = 0;

	total = f + s;
	return (total);
}
