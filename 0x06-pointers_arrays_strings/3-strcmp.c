#include "main.h"
/**
  * _strcmp - compares two string
  * @s1: receives an adress of a first string
  * @s2: receives an address of second string
  * Return: an integer
  */

int _strcmp(char *s1, char *s2)
{
	return (strcmp(s1, s2));
}
